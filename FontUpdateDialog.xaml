<Window x:Class="Wpf_gdRunnerLite.FontUpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="字体下载" Width="760" Height="620" MinWidth="680" MinHeight="520"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="CanResize"
        Background="#0B0D11" ShowInTaskbar="False" FontFamily="{StaticResource UiFont}"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="42" ResizeBorderThickness="5" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Foreground" Value="#C7CDD5"/>
            <Setter Property="Background" Value="#171B21"/>
            <Setter Property="BorderBrush" Value="#3A424D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="B" Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="B" Property="Background" Value="#232A33"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="B" Property="Opacity" Value="0.48"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DownloadButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#286FAE"/>
            <Setter Property="BorderBrush" Value="#4C97D7"/>
        </Style>
        <Style x:Key="DownloadProgressStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="7"/>
            <Setter Property="Background" Value="#252B33"/>
            <Setter Property="Foreground" Value="#368DDB"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="3"/>
                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" Background="{TemplateBinding Foreground}" CornerRadius="3"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#30FFFFFF" BorderThickness="1" Background="#0E1116">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="64"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#C90B0E13" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="42"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Margin="15,0,0,0" VerticalAlignment="Center">
                    <TextBlock Foreground="#4C97D7" FontFamily="Segoe MDL2 Assets" FontSize="15" Text="&#xE896;"/>
                    <TextBlock Margin="10,0,0,0" Foreground="#E6E9ED" FontSize="12" FontWeight="SemiBold" Text="字体包更新"/>
                </StackPanel>
                <Button x:Name="TitleCloseButton" Grid.Column="1" Width="42" Height="42" WindowChrome.IsHitTestVisibleInChrome="True" Foreground="#BDC4CC" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="CloseButton_Click">
                    <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="10" Text="&#xE8BB;"/>
                </Button>
            </Grid>

            <Grid Grid.Row="1" Margin="24,20,24,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel>
                    <TextBlock Foreground="#F3F0E9" FontSize="21" FontWeight="SemiBold" Text="可下载的字体更新"/>
                    <TextBlock x:Name="SummaryText" Margin="0,7,0,0" Foreground="#858F9A" FontSize="11" TextWrapping="Wrap" Text="下载完成后会校验 SHA-256，并保存到程序目录的 AssetsFontBundle。"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" Margin="0,18,0,0" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,12" Padding="17" Background="#141920" BorderBrush="#32FFFFFF" BorderThickness="1" CornerRadius="11">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Foreground="#F2F4F6" FontSize="15" FontWeight="SemiBold" Text="{Binding Package.Name}"/>
                                                <TextBlock Margin="9,2,0,0" Foreground="#6E9FCD" FontSize="10.5" Text="{Binding Package.EnglishName}"/>
                                                <Border Margin="10,0,0,0" Padding="7,3" Background="#203B5771" BorderBrush="#3A75A9D2" BorderThickness="1" CornerRadius="9">
                                                    <TextBlock Foreground="#83B9E8" FontSize="9.5" Text="{Binding VersionLabel}"/>
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Margin="0,7,18,0" Foreground="#87919C" FontSize="10.5" TextWrapping="Wrap" Text="{Binding Package.Description}"/>
                                            <TextBlock Margin="0,8,18,0" Foreground="#66717D" FontSize="9.8" Text="{Binding DifferenceText}"/>
                                        </StackPanel>
                                        <Button Grid.Column="1" Tag="{Binding Package.Id}" IsEnabled="{Binding CanDownload}" Style="{StaticResource DownloadButtonStyle}" Click="DownloadButton_Click">
                                            <TextBlock Text="{Binding ButtonText}"/>
                                        </Button>

                                        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,14,0,0" Visibility="{Binding ProgressVisibility}">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Foreground="#91A0AE" FontSize="9.8" Text="{Binding ProgressText}"/>
                                            <TextBlock Grid.Column="1" Margin="12,0,0,0" Foreground="#69AEE8" FontSize="9.8" FontWeight="SemiBold" Text="{Binding PercentageText}"/>
                                            <ProgressBar Grid.Row="1" Grid.ColumnSpan="2" Margin="0,7,0,0" Minimum="0" Maximum="100" Value="{Binding ProgressValue}" Style="{StaticResource DownloadProgressStyle}"/>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>

            <Border Grid.Row="2" BorderBrush="#24FFFFFF" BorderThickness="0,1,0,0" Background="#0C0F14">
                <Grid Margin="24,0">
                    <TextBlock x:Name="FooterStatusText" VerticalAlignment="Center" Foreground="#697480" FontSize="10.5" Text="可以逐个下载需要的字体包。"/>
                    <Button x:Name="FooterCloseButton" Width="96" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource DialogButtonStyle}" Click="CloseButton_Click" Content="关闭"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
